{
	"name": "durable-object-starter",
	"type": "module",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "esbuild src/index.ts --bundle --sourcemap --outfile=dist/_worker.js --format=esm --platform=browser --jsx=automatic --jsx-import-source=hono/jsx --external:cloudflare:* --external:workerd:*",
		"pages:deploy": "pnpm run build && wrangler pages deploy dist",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"esbuild": "^0.25.9",
		"typescript": "^5.5.2",
		"wrangler": "^4.28.0"
	},
	"dependencies": {
		"hono": "^4.8.12"
	}
}